<li>
	{{burger_name}}

	{{#if devour}}
	<button class="change-devour" data-id="{{id}}" data-nowdevour="{{devour}}">
		Devour!
	</button>
	{{else}}
	<button class="delete-burger" data-id="{{this.id}}">Delete</button>
	{{/if}}

</li>